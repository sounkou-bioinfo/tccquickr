% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tccq_compile.R
\name{tccq_compile}
\alias{tccq_compile}
\title{Experimental tccq R-to-C compiler entry point}
\usage{
tccq_compile(
  fn,
  mode = c("compile", "code", "ir"),
  fallback = c("hard", "auto"),
  backend = tccq_backend_tinycc(),
  target = tccq_target_c_rapi(),
  extlibs = list(),
  debug = FALSE
)
}
\arguments{
\item{fn}{R function with a leading declare(type(...)) annotation.}

\item{mode}{One of "compile", "code", or "ir".}

\item{fallback}{One of "hard" or "auto". In "hard" mode unsupported calls
are rejected. In "auto" mode unsupported calls may lower to explicit
\code{r_eval} boundary nodes.}

\item{backend}{Backend object. Use \code{tccq_backend_source()},
\code{tccq_backend_tinycc()}, or \code{tccq_backend_shlib()}. Defaults to TinyCC via
\code{Rtinycc}.}

\item{target}{Target object. Defaults to C + R C API emission.}

\item{extlibs}{Optional list of tccq_external_library descriptors.}

\item{debug}{Print generated C source before compiling.}
}
\value{
A compiled callable, C source string, or module IR depending on mode.
}
\description{
This is the package's current compiler path. The current milestone supports declared
scalar and vector arithmetic, generic fold-style reducers, local bindings,
indexed reads, contiguous slices, and local indexed/range writes.
}
\details{
Current statement semantics are intentionally strict: rebinding a local name
is rejected, direct mutation of formal arguments is rejected, and indexed or
range assignment currently requires a scalar right-hand side.
}
