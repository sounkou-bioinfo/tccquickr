% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tccq_jit.R
\name{tccq_jit}
\alias{tccq_jit}
\title{Runtime-specialized JIT dispatcher for declared-typed \code{tccq} functions}
\usage{
tccq_jit(
  fn,
  fallback = c("hard", "auto"),
  backend = tccq_backend_tinycc(),
  target = tccq_target_c_rapi(),
  extlibs = list(),
  debug = FALSE,
  exact = TRUE
)
}
\arguments{
\item{fn}{R function with a leading \code{declare(type(...))} annotation.}

\item{fallback}{One of "hard" or "auto". In "hard" mode unsupported calls
are rejected. In "auto" mode unsupported calls may lower to explicit
\code{r_eval} boundary nodes.}

\item{backend}{Backend object. Use \code{tccq_backend_source()},
\code{tccq_backend_tinycc()}, or \code{tccq_backend_shlib()}.}

\item{target}{Target object. Defaults to C + R C API emission.}

\item{extlibs}{Optional list of \code{tccq_external_library()} descriptors.}

\item{debug}{Print generated C source before each (first-time) signature
compilation.}

\item{exact}{If \code{TRUE}, each signature is specialized on exact observed shape
(\code{length} for rank-1 and \code{nrow}/\code{ncol} for rank-2) and corresponding
argument checks are emitted into C entry setup.}
}
\value{
A callable R function with signature-cache semantics. Repeated calls
with the same observed signature reuse the compiled callable.
}
\description{
\code{tccq_jit()} wraps a declared \code{tccq} function with a per-call dispatch
cache keyed by observed argument signatures. Each distinct signature is compiled
at most once (optionally with exact shape guards) and then reused on
subsequent calls.
}
\details{
This is intentionally a thin layer over \code{tccq_compile()} and is aimed at
low-latency interactive workflows where many calls share the same runtime
shape, e.g. fixed-size vectors/matrices.
}
\examples{
f <- function(x) {
  declare(type(x = double(NA)))
  x + 1
}
jf <- tccq_jit(f, exact = TRUE)
jf(c(1, 2, 3))

}
\seealso{
\code{\link[=tccq_compile]{tccq_compile()}}, \code{\link[=tccq_backend_tinycc]{tccq_backend_tinycc()}}, \code{\link[=tccq_backend_shlib]{tccq_backend_shlib()}}
}
